name: dari_yemen
description: "تطبيق داري - المنصة العقارية في اليمن"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
    supabase_flutter: ^2.8.0
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
